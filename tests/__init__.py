"""vllm-rdna-docker test package.

Pytest imports the tooling under test via ``from tools.validate import ...``;
``vllm-rdna-docker/`` (which has no ``__init__.py``) is inserted into
``sys.path`` by pytest's package rootdir handling.
"""
