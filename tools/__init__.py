"""vllm-rdna-docker tooling package.

Currently exposes the strict TOML config validator (``tools.validate``).
Later todos add the resolver, build CLI, verification, and publication tools
as sibling modules in this package.
"""
